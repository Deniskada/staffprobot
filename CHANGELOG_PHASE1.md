# Changelog: Фаза 1 — Единый сервис загрузки медиа

**Дата:** 26 января 2026  
**Версия:** restruct1 Phase 1  
**Ветка:** `plan/restruct1`

---

## 🎯 Обзор

Реализован единый сервис загрузки медиа с поддержкой Telegram и Object Storage (S3/Selectel), настройки хранилища для владельцев, интеграция с биллингом и загрузка фото профилей.

---

## ✨ Новые возможности

### 📦 Единый сервис загрузки медиа

- **Абстракция хранилища**: Реализован `MediaStorageClient` (ABC) с поддержкой Telegram и S3-совместимых хранилищ
- **Провайдеры**: 
  - `TelegramMediaStorageClient` — хранение через Telegram file_id
  - `S3MediaStorageClient` — хранение в MinIO (dev) и Selectel Object Storage (prod)
- **Фабрика провайдеров**: Автоматический выбор провайдера на основе настроек
- **Presigned URLs**: Безопасный доступ к файлам через прокси-роут `/api/media/{key:path}`

### 🔒 Опция «Использовать защищённое хранилище файлов»

- **Системная функция**: Добавлена опция `secure_media_storage` в `SystemFeatures`
- **Настройки по владельцу**: Таблица `owner_media_storage_options` для выбора хранилища по контекстам:
  - Задачи (Tasks v2)
  - Отмены смен
  - Инциденты
  - Договоры (подготовка к Фазе 2)
- **Режимы хранения**: Для каждого контекста можно выбрать:
  - `telegram` — только Telegram
  - `storage` — только Object Storage
  - `both` — оба хранилища одновременно
- **UI**: Страница настроек `/owner/profile/media-storage` с удобным интерфейсом выбора

### 💰 Интеграция с биллингом

- **Цены хранения**: Добавлены поля в `TariffPlan`:
  - `storage_price_telegram` — цена хранения в Telegram (₽/мес)
  - `storage_price_object_storage` — цена хранения в S3 (₽/мес)
  - `storage_option_price` — цена опции «Защищённое хранилище» (₽/мес)
- **Лог изменений**: Таблица `subscription_option_log` для отслеживания:
  - Изменений тарифных планов (`old_tariff_id`, `new_tariff_id`)
  - Включения/выключения опций (`options_enabled`, `options_disabled`)
- **Расчёт стоимости**: Метод `BillingService.compute_subscription_amount()` учитывает:
  - Базовую цену тарифа
  - Доплату за опцию «Защищённое хранилище» (если включена)
- **Админка**: Просмотр логов изменений подписки на странице `/admin/subscriptions/{id}/logs`

### 📸 Загрузка фото профилей

- **Владелец**: Загрузка фото компании на странице `/owner/profile`
  - Автоматическое сохранение в профиль через autosave
  - Поддержка до 5 фотографий
- **Сотрудник**: Загрузка аватара на странице `/employee/profile`
  - Модальное окно для выбора фото
  - Автоматическое обновление аватара на странице
- **Хранение**: Фото сохраняются в папках `profiles/{user_id}/` и `profiles/{user_id}/photos/`

### 🔄 Улучшения отмены смен

- **Медиа отмен**: Таблица `shift_cancellation_media` для хранения файлов отмены
- **Двойное хранение**: При режиме `both` медиа сохраняется и в Telegram, и в S3
  - Поле `telegram_file_id` в `ShiftCancellationMedia` для отслеживания файлов в Telegram
- **Отображение**: На страницах `/owner/cancellations` и `/owner/shifts/{id}` показываются ссылки на оба хранилища (если доступны)

---

## 🔧 Технические изменения

### База данных

- **Новые таблицы**:
  - `owner_media_storage_options` — настройки хранилища по владельцу
  - `subscription_option_log` — лог изменений тарифов и опций
  - `shift_cancellation_media` — медиа-файлы отмены смен
- **Изменения в таблицах**:
  - `users`: добавлено поле `avatar_url` для URL фото профиля
  - `tariff_plans`: добавлены поля `storage_price_telegram`, `storage_price_object_storage`, `storage_option_price`
  - `shift_cancellation_media`: добавлено поле `telegram_file_id`

### Миграции

- `b2c3d4e5f6g8` — создание `owner_media_storage_options` и добавление функции `secure_media_storage`
- `c3d4e5f6g7h9` — добавление цен хранения в тарифы и создание `subscription_option_log`
- `37f7f5f15c0a` — добавление `telegram_file_id` в `shift_cancellation_media`
- `da1eedda616f` — добавление `avatar_url` в `users`

### Новые сервисы

- `OwnerMediaStorageService` — управление настройками хранилища владельца
- `MediaStorageClient` (ABC) — абстракция для работы с хранилищами
- `S3MediaStorageClient` — реализация для S3-совместимых хранилищ
- `TelegramMediaStorageClient` — реализация для Telegram

### Новые роуты

- `/owner/profile/media-storage` — настройки хранилища
- `/owner/profile/api/photo` — загрузка фото компании
- `/employee/api/profile/avatar` — загрузка аватара сотрудника
- `/api/media/{key:path}` — прокси для доступа к файлам из Object Storage
- `/admin/subscriptions/{id}/logs` — просмотр логов изменений подписки

### Обновления

- `MediaOrchestrator` — поддержка режима `both` для загрузки в оба хранилища
- `BillingService` — учёт опций в расчёте стоимости подписки
- `ShiftCancellationService` — сохранение медиа с поддержкой `telegram_file_id`

---

## 🐛 Исправления

- Исправлена обработка ошибок при загрузке медиа
- Исправлено логирование (убраны неподдерживаемые keyword arguments)
- Исправлена генерация URL для доступа к файлам (учёт dev/prod окружений)
- Исправлена загрузка фото в Telegram группу при отмене смены через бота

---

## 📝 Документация

- Обновлена документация по медиа-сервису
- Добавлена инструкция по ручному тестированию (`tests/manual/RESTRUCT1_PHASE1_MANUAL_TESTING.md`)
- Обновлена документация по биллингу и тарифам

---

## 🧪 Тестирование

- Unit-тесты для `BillingService` (расчёт стоимости, логирование опций)
- Unit-тесты для `OwnerMediaStorageService`
- Ручное тестирование всех сценариев загрузки и отображения медиа

---

## 📍 Адреса объектов: карта и синхронизация полей

- Формы создания и редактирования объектов (`/owner/objects/create`, `/owner/objects/{id}/edit`) переведены на общий компонент выбора адреса с картой Яндекс (`shared/address_map.html` + `address_map.js`) во вкладке «Адрес».
- Скрытое поле `address` теперь автоматически заполняется выбранным адресом и синхронизируется со строкой поиска как при выборе подсказки, так и перед сохранением формы.
- Координаты объекта (`latitude`, `longitude`) обновляются при выборе точки на карте и могут быть откорректированы вручную.
- Выбранные адреса дополнительно сохраняются в таблицу `addresses` через API (`POST /api/addresses`) с привязкой к пользователю.

---

## 🔗 Связанные задачи

- Фаза 1.1–1.7: Единый сервис загрузки медиа (завершено)
- Подготовка к Фазе 2: Типы договоров, шаблоны, мастер заключения

---

## 📌 Примечания

- Существующие медиа в Telegram не мигрируются (обратная совместимость)
- Новые медиа загружаются согласно настройкам владельца
- Presigned URLs имеют срок действия 1 год для фото профилей, 1 час для остальных медиа
- Прокси-роут `/api/media/` необходим для доступа к файлам из MinIO на dev-окружении
