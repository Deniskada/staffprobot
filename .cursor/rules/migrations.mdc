# Правила работы с миграциями и системными проблемами

## 🗄️ Миграции базы данных

### Обязательные правила

#### 1. Проверка миграций перед применением
- **ВСЕГДА** проверяй содержимое созданной миграции перед применением
- Если миграция содержит операции с системными таблицами PostGIS (`spatial_ref_sys`, `geometry_columns`, `spatial_ref_sys`), **НЕМЕДЛЕННО** исправляй её
- Удаляй из миграции все операции с таблицами PostGIS, если они не нужны

#### 2. Создание миграций
```bash
# Создание миграции
docker compose -f docker-compose.dev.yml exec web alembic revision --autogenerate -m "описание_изменений"

# Проверка содержимого миграции
cat migrations/versions/[имя_файла].py

# Применение миграции
docker compose -f docker-compose.dev.yml exec web alembic upgrade head
```

#### 3. Исправление неправильных миграций
Если миграция содержит операции с PostGIS:
```python
# Удалить эти строки:
op.drop_table('spatial_ref_sys')
op.create_table('spatial_ref_sys', ...)

# Оставить только нужные операции:
op.add_column('table_name', sa.Column('column_name', sa.Type(), nullable=True))
op.create_index(...)
op.create_foreign_key(...)
```

### Типичные проблемы и решения

#### Проблема: "cannot drop table spatial_ref_sys because extension postgis requires it"
**Причина**: Alembic пытается удалить системную таблицу PostGIS
**Решение**: 
1. Открыть файл миграции
2. Удалить все операции с `spatial_ref_sys`
3. Оставить только нужные изменения схемы

#### Проблема: "Mapper has no property 'relationship_name'"
**Причина**: Циклические зависимости в SQLAlchemy relationships
**Решение**:
1. Временно закомментировать проблемные `relationship`
2. Убрать `back_populates` из связанных моделей
3. После решения основной задачи восстановить relationships

## 🐳 Docker и контейнеры

### Обязательные правила

#### 1. Запуск команд в контейнерах
- **ВСЕГДА** используй `docker compose -f docker-compose.dev.yml exec [service]` для команд внутри контейнеров
- Для миграций: `docker compose -f docker-compose.dev.yml exec web alembic ...`
- Для работы с БД: `docker compose -f docker-compose.dev.yml exec postgres psql ...`

#### 2. Перезапуск сервисов после изменений
```bash
# После изменений в коде
docker compose -f docker-compose.dev.yml restart web bot

# После миграций БД
docker compose -f docker-compose.dev.yml restart web
```

#### 3. Проверка логов
```bash
# Логи веб-сервиса
docker compose -f docker-compose.dev.yml logs web --tail=20

# Логи бота
docker compose -f docker-compose.dev.yml logs bot --tail=20

# Логи всех сервисов
docker compose -f docker-compose.dev.yml logs --tail=20
```

## 🔧 Системные проблемы

### Проблемы с PostgreSQL

#### Проблема: "relation does not exist"
**Причина**: Таблица не создана или миграция не применена
**Решение**:
1. Проверить статус миграций: `docker compose -f docker-compose.dev.yml exec web alembic current`
2. Применить все миграции: `docker compose -f docker-compose.dev.yml exec web alembic upgrade head`

#### Проблема: "column does not exist"
**Причина**: Поле не добавлено в таблицу
**Решение**:
1. Создать миграцию для добавления поля
2. Проверить содержимое миграции
3. Применить миграцию

### Проблемы с SQLAlchemy

#### Проблема: "Mapper initialization error"
**Причина**: Циклические зависимости в relationships
**Решение**:
1. Временно закомментировать проблемные relationships
2. Использовать `backref` вместо `back_populates`
3. Или разделить relationships на отдельные файлы

#### Проблема: "relationship 'X' expects a class or a mapper argument"
**Причина**: Неправильное определение relationship
**Решение**:
1. Использовать строковые имена классов: `relationship("ClassName")`
2. Или импортировать класс перед использованием

## 📝 Процедуры отладки

### 1. Отладка API
```bash
# Проверка API
curl -s "http://localhost:8001/api/endpoint" | jq

# Проверка с параметрами
curl -s "http://localhost:8001/api/endpoint?param=value" | jq
```

### 2. Отладка базы данных
```bash
# Подключение к БД
docker compose -f docker-compose.dev.yml exec postgres psql -U postgres -d staffprobot_dev

# Проверка таблиц
\dt

# Проверка структуры таблицы
\d table_name

# Проверка данных
SELECT * FROM table_name LIMIT 5;
```

### 3. Отладка миграций
```bash
# Текущая версия
docker compose -f docker-compose.dev.yml exec web alembic current

# История миграций
docker compose -f docker-compose.dev.yml exec web alembic history

# Откат миграции
docker compose -f docker-compose.dev.yml exec web alembic downgrade -1
```

## 🚨 Критические правила

### НИКОГДА НЕ ДЕЛАЙ:
1. ❌ Не применяй миграции без проверки содержимого
2. ❌ Не удаляй системные таблицы PostGIS
3. ❌ Не запускай команды миграций вне Docker контейнера
4. ❌ Не коммить непроверенные миграции
5. ❌ Не игнорируй ошибки миграций

### ВСЕГДА ДЕЛАЙ:
1. ✅ Проверяй содержимое миграции перед применением
2. ✅ Используй Docker для всех команд БД
3. ✅ Проверяй логи после изменений
4. ✅ Тестируй API после изменений
5. ✅ Делай коммиты с понятными сообщениями

## 📋 Чек-лист перед коммитом

- [ ] Миграция проверена и не содержит операций с PostGIS
- [ ] Все команды запущены через Docker
- [ ] API работает корректно
- [ ] Логи не содержат критических ошибок
- [ ] Изменения протестированы
- [ ] Коммит имеет понятное описание

## 🔄 Процедура добавления новых правил

При столкновении с повторяющейся системной проблемой:

1. **Зафиксируй проблему** - опиши симптом и причину
2. **Найди решение** - определи правильную последовательность действий
3. **Добавь в этот файл** - создай новый раздел или дополни существующий
4. **Обнови чек-лист** - добавь проверку в список обязательных действий
5. **Поделись с командой** - убедись, что все знают о новом правиле

### Формат добавления новых правил:
```markdown
#### Проблема: "описание проблемы"
**Причина**: объяснение причины
**Решение**: 
1. Шаг 1
2. Шаг 2
3. Шаг 3
```

---

**Важно**: Этот файл должен обновляться при каждой новой системной проблеме. Не игнорируй повторяющиеся ошибки - фиксируй их здесь!