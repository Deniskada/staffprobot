---
alwaysApply: false
---
# Правила выполнения работ по тасклисту StaffProBot

## 🐳 Docker-окружение (ОБЯЗАТЕЛЬНО)

### Контейнеры и сервисы
**ВАЖНО**: Проект работает ТОЛЬКО через Docker Compose!

#### Локальная разработка (dev):
- **Основной файл**: `docker-compose.dev.yml`
- **Команда запуска**: `docker compose -f docker-compose.dev.yml up -d`
- **Команда остановки**: `docker compose -f docker-compose.dev.yml down`
- **Контейнеры**:
  - `staffprobot-web-1` - веб-приложение (FastAPI + Jinja2)
  - `staffprobot-postgres-1` - база данных PostgreSQL
  - `staffprobot-redis-1` - кэш и очереди
  - `staffprobot-celery-worker-1` - воркер Celery
  - `staffprobot-celery-beat-1` - планировщик Celery

#### Production (прод):
- **Основной файл**: `docker-compose.prod.yml`
- **Команда запуска**: `docker compose -f docker-compose.prod.yml up -d`
- **Команда остановки**: `docker compose -f docker-compose.prod.yml down`
- **Контейнеры**: аналогичные, но с суффиксом `-prod`

### Выполнение команд
**ВСЕГДА используй Docker для выполнения команд:**

#### Тестирование кода:
```bash
# Локально (dev)
docker compose -f docker-compose.dev.yml exec web python -c "код"

# На проде
ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && docker compose -f docker-compose.prod.yml exec web python -c "код"'
```

#### Работа с базой данных:
```bash
# Локально (dev)
docker compose -f docker-compose.dev.yml exec postgres psql -U postgres -d staffprobot_dev

# На проде
ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && docker compose -f docker-compose.prod.yml exec postgres psql -U postgres -d staffprobot_prod'
```

#### Просмотр логов:
```bash
# Локально (dev)
docker compose -f docker-compose.dev.yml logs web
docker compose -f docker-compose.dev.yml logs postgres

# На проде
ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && docker compose -f docker-compose.prod.yml logs web'
```

### База данных
- **Dev**: `staffprobot_dev`
- **Prod**: `staffprobot_prod`
- **Пользователь**: `postgres`
- **Хост**: `postgres` (внутри Docker сети)

### Переменные окружения
- **Файл**: `.env` (в корне проекта)
- **Используется**: и в dev, и в prod
- **Docker**: автоматически подхватывает переменные из `.env`

## 🎯 Основные принципы

**ВАЖНО**: Эти правила основаны на принципах из `vision.md` и должны соблюдаться при разработке. При возникновении вопросов обращайся к основному документу видения проекта.

## 📋 Обязательный порядок выполнения

### 1. Строгое следование плану
- **НЕ ПЕРЕПРЫГИВАТЬ** между итерациями
- Выполнять задачи **ТОЛЬКО** в порядке, указанном в `@tasklist.md`
- Каждая итерация должна быть **ПОЛНОСТЬЮ** завершена перед переходом к следующей

### 2. Согласование решения перед реализацией
**ПЕРЕД** началом работы над любой задачей:
- Предложить предполагаемое решение с примерами кода
- Объяснить архитектурные решения
- Показать структуру файлов и компонентов
- Дождаться **ПОДТВЕРЖДЕНИЯ** от пользователя

### 3. Реализация после согласования
- Реализовывать **ТОЛЬКО** после получения подтверждения
- Следовать принципам из `@vision.md`
- Использовать технологии и подходы, указанные в техническом видении

### 4. Ожидание подтверждения после реализации
- После завершения задачи показать результат
- Дождаться **ПОДТВЕРЖДЕНИЯ** выполнения
- **НЕ ПЕРЕХОДИТЬ** к следующей задаче без подтверждения

## 🔄 Рабочий процесс для каждой итерации

### Шаг 1: Анализ итерации
```
1. Изучить задачи итерации в @tasklist.md
2. Проанализировать зависимости и требования
3. Определить архитектурные решения
4. Подготовить план реализации
```

### Шаг 2: Согласование решения
```
1. Предложить предполагаемое решение
2. Показать структуру кода и компонентов
3. Объяснить выбор технологий
4. Дождаться подтверждения пользователя
```

### Шаг 3: Реализация
```
1. Создать необходимые файлы и структуру
2. Реализовать функционал согласно согласованному плану
3. Следовать принципам из @vision.md
4. Применять правила из @conventions.mdc
```

### Шаг 4: Демонстрация результата
```
1. Показать созданный код
2. Объяснить реализованную функциональность
3. Дождаться подтверждения выполнения
4. Обновить прогресс в @tasklist.md
```

### Шаг 5: Переход к следующей задаче
```
1. Получить подтверждение готовности к следующей задаче
2. Перейти к следующей задаче итерации
3. Повторить цикл для новой задачи
```

## 📊 Обновление прогресса

### После каждой выполненной задачи:
- [x] Отметить задачу как выполненную в `doc/plans/roadmap.md`
- [x] Обновить процент выполнения в шапке roadmap
- [x] Обновить соответствующие файлы в `doc/plans/tracks/` и `doc/plans/entities/`
- [x] Создать коммит с описанием изменений

### Формат обновления roadmap:
```markdown
# В шапке файла:
**Общий прогресс:** 222/232 (95.7%)

# В итерации:
- [x] **1.1. Исправить ошибку создания договора с пустым сообщением (1 день)**
  - Type: bug | Files: apps/web/services/contract_service.py, apps/web/routes/employees.py
  - Acceptance: при попытке создать договор с существующим активным договором выводится понятное сообщение об ошибке
```

## 🚫 Запрещенные действия

- **НЕ НАЧИНАТЬ** реализацию без согласования решения
- **НЕ ПЕРЕХОДИТЬ** к следующей итерации без завершения текущей
- **НЕ ИГНОРИРОВАТЬ** принципы из `@vision.md`
- **НЕ ОТКЛОНЯТЬСЯ** от структуры проекта, указанной в техническом видении
- **НЕ ПРОПУСКАТЬ** этап согласования решения

## ✅ Критерии готовности итерации

Итерация считается завершенной, когда:
- [x] Все задачи итерации выполнены
- [x] Функционал протестирован в реальных условиях
- [x] Код покрыт тестами (минимум 80%)
- [x] Документация обновлена
- [x] Отчет по прогрессу обновлен в `@tasklist.md`

## 🔗 Связь с документацией

### Основные документы:
- **`@idea.md`** - концепция и бизнес-логика проекта
- **`@tasklist.md`** - план разработки и прогресс
- **`@vision.md`** - техническое видение и архитектура
- **`@conventions.mdc`** - правила кодирования

### Приоритет документов:
1. **`@tasklist.md`** - план выполнения (главный документ)
2. **`@vision.md`** - технические решения и архитектура
3. **`@conventions.mdc`** - правила кодирования
4. **`@idea.md`** - бизнес-контекст и требования

## 📝 Примеры взаимодействия

### Пример согласования решения:
```
👨‍💻 Ассистент: "Для задачи 'Создать структуру проекта' предлагаю:
1. Создать папки: apps/, core/, domain/, infrastructure/
2. Настроить Docker Compose с PostgreSQL и Redis
3. Создать базовую конфигурацию

Согласны ли вы с таким подходом?"

👤 Пользователь: "Да, согласен. Реализуй."

👨‍💻 Ассистент: "Отлично! Реализую структуру проекта..."
```

### Пример подтверждения выполнения:
```
👨‍💻 Ассистент: "Структура проекта создана:
- Созданы все необходимые папки
- Настроен Docker Compose
- Добавлена базовая конфигурация

Задача выполнена. Подтвердите, чтобы перейти к следующей."

👤 Пользователь: "Подтверждаю. Переходи к следующей задаче."

👨‍💻 Ассистент: "Перехожу к задаче 'Настроить логирование и мониторинг'..."
```

## 💾 Создание коммитов

### Правила коммитов:
- **Всегда на русском языке** - краткие, понятные сообщения
- **Формат**: `Тип: Краткое описание изменения`
- **Примеры**:
  - `Исправление: ошибка создания договора с пустым сообщением`
  - `Добавление: проверка активных договоров при создании`
  - `Обновление: обработка ошибок в роуте сотрудников`

### Команды:
```bash
# Добавить файлы
git add apps/web/services/contract_service.py apps/web/routes/employees.py

# Создать коммит
git commit -m "Исправление: ошибка создания договора с пустым сообщением"

# Отправить изменения
git push origin main
```

## 🎯 Итоговые правила

1. **СЛЕДУЙ ПЛАНУ** - строго по `doc/plans/roadmap.md`
2. **СОГЛАСУЙ РЕШЕНИЕ** - перед каждой реализацией
3. **РЕАЛИЗУЙ** - после подтверждения
4. **ДОЖДИСЬ ПОДТВЕРЖДЕНИЯ** - после выполнения
5. **ОБНОВИ ПРОГРЕСС** - в `doc/plans/roadmap.md`
6. **СОЗДАЙ КОММИТ** - с описанием изменений
7. **ПЕРЕХОДИ ДАЛЬШЕ** - только после согласования

**Помни**: Качество важнее скорости. Каждый шаг должен быть согласован и подтвержден!

---

## 🚀 Деплой и обновление статики (обязательно)

### Кэш статики и версионирование
- В шаблонах Jinja2 использовать фильтр `static_version` для всех CSS/JS:
  - Пример JS: `{{ 'js/shared/universal_calendar.js' | static_version }}`
  - Пример CSS: `{{ 'css/shared/calendar.css' | static_version }}`
- Не использовать прямые `src="/static/..."` или `href="/static/..."` без версии.
- На проде разрешается агрессивное кэширование статики (Cache-Control: max-age=31536000, immutable), т.к. `?v=` меняется при каждом обновлении файла.
- Любые расхождения «локально обновилось, на проде нет» решаются проверкой, что в шаблон добавлен `| static_version` и файл действительно перегенерился (mtime/хэш).

### Процедура деплоя на production (Docker Compose)
**ВАЖНО**: На проде используется Docker Compose, НЕ systemd сервисы!

1) Подключение по SSH (одинарные кавычки обязательны):
   - `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && git fetch origin && git checkout main && git pull origin main'`

2) Перезапуск контейнеров (основной способ):
   - `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && docker compose -f docker-compose.prod.yml down && docker compose -f docker-compose.prod.yml up -d'`

3) Проверка статуса:
   - `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && docker compose -f docker-compose.prod.yml ps'`

### Работа с базой данных через Docker

#### Подключение к БД прода:
- **База**: `staffprobot_prod` (НЕ `staffprobot`)
- **Пользователь**: `postgres` (НЕ `staffprobot`)
- **Команда**: `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && docker compose -f docker-compose.prod.yml exec postgres psql -U postgres -d staffprobot_prod'`

#### Создание бэкапа прода:
- `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && docker compose -f docker-compose.prod.yml exec postgres pg_dump -U postgres -d staffprobot_prod > /tmp/staffprobot_prod_backup_$(date +%Y%m%d_%H%M%S).sql'`

#### Развертывание бэкапа на дэве:
- Скопировать бэкап: `scp staffprobot@staffprobot.ru:/tmp/staffprobot_prod_backup_*.sql /home/sa/projects/staffprobot/`
- Очистить дэв БД: `docker compose -f docker-compose.dev.yml exec postgres psql -U postgres -d staffprobot_dev -c "DROP SCHEMA public CASCADE; CREATE SCHEMA public;"`
- Восстановить: `docker compose -f docker-compose.dev.yml exec -T postgres psql -U postgres -d staffprobot_dev < staffprobot_prod_backup_*.sql`

### Environment файлы

#### На проде:
- **Основной**: `.env` (в корне проекта)
- **Docker**: `docker-compose.prod.yml` использует переменные из `.env`

#### На дэве:
- **Основной**: `.env` (в корне проекта) 
- **Docker**: `docker-compose.dev.yml` использует переменные из `.env`

#### Чтение переменных БД:
- **DB_NAME**: имя базы (`staffprobot_prod` для прода, `staffprobot_dev` для дэва)
- **DB_USER**: пользователь БД (`postgres`)
- **DB_PASSWORD**: пароль из `.env` файла
- **DB_HOST**: хост БД (`postgres` внутри Docker сети)

**ВАЖНО**: Всегда читать настройки БД из переменных окружения, НЕ хардкодить!

### Обязательные проверки после деплоя
- Открыть ключевые страницы и убедиться, что в ссылках на статику присутствует `?v=...`.
- При необходимости выполнить `nginx -t && sudo systemctl reload nginx` (если менялась конфигурация nginx).

