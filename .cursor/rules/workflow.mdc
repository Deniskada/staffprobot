---
alwaysApply: true
---
# Правила выполнения работ по тасклисту StaffProBot

## 🎯 Основные принципы

**ВАЖНО**: Эти правила основаны на принципах из `vision.md` и должны соблюдаться при разработке. При возникновении вопросов обращайся к основному документу видения проекта.

## 📋 Обязательный порядок выполнения

### 1. Строгое следование плану
- **НЕ ПЕРЕПРЫГИВАТЬ** между итерациями
- Выполнять задачи **ТОЛЬКО** в порядке, указанном в `@tasklist.md`
- Каждая итерация должна быть **ПОЛНОСТЬЮ** завершена перед переходом к следующей

### 2. Согласование решения перед реализацией
**ПЕРЕД** началом работы над любой задачей:
- Предложить предполагаемое решение с примерами кода
- Объяснить архитектурные решения
- Показать структуру файлов и компонентов
- Дождаться **ПОДТВЕРЖДЕНИЯ** от пользователя

### 3. Реализация после согласования
- Реализовывать **ТОЛЬКО** после получения подтверждения
- Следовать принципам из `@vision.md`
- Использовать технологии и подходы, указанные в техническом видении

### 4. Ожидание подтверждения после реализации
- После завершения задачи показать результат
- Дождаться **ПОДТВЕРЖДЕНИЯ** выполнения
- **НЕ ПЕРЕХОДИТЬ** к следующей задаче без подтверждения

## 🔄 Рабочий процесс для каждой итерации

### Шаг 1: Анализ итерации
```
1. Изучить задачи итерации в @tasklist.md
2. Проанализировать зависимости и требования
3. Определить архитектурные решения
4. Подготовить план реализации
```

### Шаг 2: Согласование решения
```
1. Предложить предполагаемое решение
2. Показать структуру кода и компонентов
3. Объяснить выбор технологий
4. Дождаться подтверждения пользователя
```

### Шаг 3: Реализация
```
1. Создать необходимые файлы и структуру
2. Реализовать функционал согласно согласованному плану
3. Следовать принципам из @vision.md
4. Применять правила из @conventions.mdc
```

### Шаг 4: Демонстрация результата
```
1. Показать созданный код
2. Объяснить реализованную функциональность
3. Дождаться подтверждения выполнения
4. Обновить прогресс в @tasklist.md
```

### Шаг 5: Переход к следующей задаче
```
1. Получить подтверждение готовности к следующей задаче
2. Перейти к следующей задаче итерации
3. Повторить цикл для новой задачи
```

## 📊 Обновление прогресса

### После каждой выполненной задачи:
- [x] Отметить задачу как выполненную в `@tasklist.md`
- [x] Обновить процент выполнения итерации
- [x] Обновить общий прогресс проекта

### Формат обновления:
```markdown
| Итерация | Статус | Завершено | Всего задач | Процент |
|-----------|--------|-----------|-------------|---------|
| **MVP** | 🟡 В процессе | 1/8 | 8 | 12.5% |
```

## 🚫 Запрещенные действия

- **НЕ НАЧИНАТЬ** реализацию без согласования решения
- **НЕ ПЕРЕХОДИТЬ** к следующей итерации без завершения текущей
- **НЕ ИГНОРИРОВАТЬ** принципы из `@vision.md`
- **НЕ ОТКЛОНЯТЬСЯ** от структуры проекта, указанной в техническом видении
- **НЕ ПРОПУСКАТЬ** этап согласования решения

## ✅ Критерии готовности итерации

Итерация считается завершенной, когда:
- [x] Все задачи итерации выполнены
- [x] Функционал протестирован в реальных условиях
- [x] Код покрыт тестами (минимум 80%)
- [x] Документация обновлена
- [x] Отчет по прогрессу обновлен в `@tasklist.md`

## 🔗 Связь с документацией

### Основные документы:
- **`@idea.md`** - концепция и бизнес-логика проекта
- **`@tasklist.md`** - план разработки и прогресс
- **`@vision.md`** - техническое видение и архитектура
- **`@conventions.mdc`** - правила кодирования

### Приоритет документов:
1. **`@tasklist.md`** - план выполнения (главный документ)
2. **`@vision.md`** - технические решения и архитектура
3. **`@conventions.mdc`** - правила кодирования
4. **`@idea.md`** - бизнес-контекст и требования

## 📝 Примеры взаимодействия

### Пример согласования решения:
```
👨‍💻 Ассистент: "Для задачи 'Создать структуру проекта' предлагаю:
1. Создать папки: apps/, core/, domain/, infrastructure/
2. Настроить Docker Compose с PostgreSQL и Redis
3. Создать базовую конфигурацию

Согласны ли вы с таким подходом?"

👤 Пользователь: "Да, согласен. Реализуй."

👨‍💻 Ассистент: "Отлично! Реализую структуру проекта..."
```

### Пример подтверждения выполнения:
```
👨‍💻 Ассистент: "Структура проекта создана:
- Созданы все необходимые папки
- Настроен Docker Compose
- Добавлена базовая конфигурация

Задача выполнена. Подтвердите, чтобы перейти к следующей."

👤 Пользователь: "Подтверждаю. Переходи к следующей задаче."

👨‍💻 Ассистент: "Перехожу к задаче 'Настроить логирование и мониторинг'..."
```

## 🎯 Итоговые правила

1. **СЛЕДУЙ ПЛАНУ** - строго по `@tasklist.md`
2. **СОГЛАСУЙ РЕШЕНИЕ** - перед каждой реализацией
3. **РЕАЛИЗУЙ** - после подтверждения
4. **ДОЖДИСЬ ПОДТВЕРЖДЕНИЯ** - после выполнения
5. **ОБНОВИ ПРОГРЕСС** - в `@tasklist.md`
6. **ПЕРЕХОДИ ДАЛЬШЕ** - только после согласования

**Помни**: Качество важнее скорости. Каждый шаг должен быть согласован и подтвержден!

---

## 🚀 Деплой и обновление статики (обязательно)

### Кэш статики и версионирование
- В шаблонах Jinja2 использовать фильтр `static_version` для всех CSS/JS:
  - Пример JS: `{{ 'js/shared/universal_calendar.js' | static_version }}`
  - Пример CSS: `{{ 'css/shared/calendar.css' | static_version }}`
- Не использовать прямые `src="/static/..."` или `href="/static/..."` без версии.
- На проде разрешается агрессивное кэширование статики (Cache-Control: max-age=31536000, immutable), т.к. `?v=` меняется при каждом обновлении файла.
- Любые расхождения «локально обновилось, на проде нет» решаются проверкой, что в шаблон добавлен `| static_version` и файл действительно перегенерился (mtime/хэш).

### Процедура деплоя на production (git pull)
1) Подключение по SSH (одинарные кавычки обязательны):
   - `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && git fetch origin && git checkout main && git pull origin main'`
2) Обновление зависимостей (если требуется):
   - `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && source venv/bin/activate && pip install -r requirements.txt'`
3) Миграции БД:
   - `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && alembic upgrade head'`
4) Перезапуск сервисов приложения:
   - `ssh staffprobot@staffprobot.ru 'sudo systemctl restart staffprobot'` (или соответствующие unit-файлы)
5) Проверка статуса:
   - `ssh staffprobot@staffprobot.ru 'systemctl status staffprobot --no-pager'`

Альтернатива (Docker Compose):
- `ssh staffprobot@staffprobot.ru 'cd /opt/staffprobot && docker compose -f docker-compose.prod.yml pull && docker compose -f docker-compose.prod.yml up -d'`

### Обязательные проверки после деплоя
- Открыть ключевые страницы и убедиться, что в ссылках на статику присутствует `?v=...`.
- При необходимости выполнить `nginx -t && sudo systemctl reload nginx` (если менялась конфигурация nginx).

