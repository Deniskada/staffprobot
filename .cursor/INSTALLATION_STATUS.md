# Статус установки фреймворков для StaffProBot

**Дата**: 2026-02-04  
**Статус**: Частично установлено

## ✅ Установлено

### 1. cursor-memory-bank
**Статус**: ✅ Установлено  
**Версия**: v0.8 (команды Cursor 2.0)

**Что установлено**:
- ✅ `.cursor/commands/` - команды Memory Bank (`/van`, `/plan`, `/creative`, `/build`, `/reflect`, `/archive`)
- ✅ `.cursor/memory-bank-rules/` - правила Memory Bank
- ✅ `memory-bank/` - структура для хранения памяти проекта
- ✅ Файлы Memory Bank созданы:
  - `PROJECT_BRIEF.md`
  - `ACTIVE_FOCUS.md`
  - `ARCHITECTURE.md`
  - `PATTERNS.md`
  - `TECHNICAL_CONTEXT.md`
  - `PROGRESS.md`

**Использование**:
- В Cursor набрать `/` для просмотра доступных команд
- Начать с `/van` для инициализации проекта
- Команды готовы к использованию без дополнительной настройки

### 2. OpenSpec структура
**Статус**: ✅ Структура создана  
**Тип**: Фреймворк (не требует установки)

**Что создано**:
- ✅ `specs/capabilities/` - директория для авторитетных спецификаций
- ✅ `changes/active/` - директория для активных предложений изменений
- ✅ `changes/archive/` - директория для архивированных изменений

**Следующие шаги**:
- Мигрировать ключевые спецификации из `doc/vision_v1/` в `specs/capabilities/`
- Использовать структуру для spec-driven разработки

## ⚠️ Требует установки

### 3. spec-kit
**Статус**: ✅ Установлено  
**Версия**: specify-cli 0.0.22

**Установлено через pipx**:
```bash
pipx install git+https://github.com/github/spec-kit.git
```

**Команда**: `specify` (доступна в `~/.local/bin/`)

**Использование**:
- `specify --version` - проверить версию
- `specify --help` - показать справку
- `specify init` - инициализировать проект

**После установки**:
- ✅ Команда `specify` установлена и доступна
- ⚠️ Нужно перезагрузить shell или выполнить `source ~/.bashrc` для доступа
- Создать конфигурационный файл `.spec-kit/config.yaml` (опционально)
- Использовать команды: `specify init`, `specify check`, `specify version`

### 4. Claude Task Master
**Статус**: ⚠️ Требует установки через MCP  
**Инструмент**: mcpbar доступен (версия 1.1.0)

**Установка**:
```bash
npx mcpbar@latest install eyaltoledano/claude-task-master -c claude
```

**Требования**:
- API ключ Anthropic (Claude)
- Настройка MCP серверов в Cursor Settings

**После установки**:
- Настроить API ключи в переменных окружения
- Добавить MCP сервер в конфигурацию Cursor
- Настроить синхронизацию с roadmap.md

## 📋 Резюме

### Готово к использованию
1. ✅ **cursor-memory-bank** - команды доступны в Cursor
2. ✅ **OpenSpec структура** - готова к использованию

### Требует действий
1. ⚠️ **spec-kit** - установить pip3 или uv, затем установить spec-kit
2. ⚠️ **TaskMaster** - установить через MCP, настроить API ключи

## Следующие шаги

1. **Немедленно**: Использовать Memory Bank команды (`/van`, `/plan`, и т.д.)
2. **Сегодня**: Установить pip3 и spec-kit
3. **Сегодня**: Установить TaskMaster через MCP
4. **На этой неделе**: Мигрировать спецификации в OpenSpec структуру

## Команды для быстрой установки

```bash
# 1. Установить pip3 (если нужно)
sudo apt install python3-pip

# 2. Установить spec-kit
pip3 install spec-kit

# 3. Установить TaskMaster через MCP
npx mcpbar@latest install eyaltoledano/claude-task-master -c claude

# 4. Проверить установку
spec-kit --version
```

---

**Примечание**: Memory Bank уже готов к использованию! Можно начинать работу с командами `/van`, `/plan`, и т.д.
