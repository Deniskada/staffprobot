# Исправление проблемы с остановкой отправки уведомлений о сменах

**Дата:** 18.11.2025  
**Проблема:** После первой отправки уведомления о сменах в Telegram отправка останавливалась, следующие уведомления не отправлялись.

---

## 🔍 Описание проблемы

После отправки первого уведомления о смене (например, при открытии/закрытии смены или отмене) последующие уведомления переставали отправляться. В логах наблюдались ошибки:
- `RuntimeError('Event loop is closed')`
- Уведомления со статусом `FAILED` в БД
- Отсутствие логов о создании новых уведомлений

---

## 🎯 Корневые причины

Обнаружены **две независимые проблемы**:

### Проблема #1: Отсутствие обработки исключений
**Файл:** `shared/services/shift_notification_service.py`

**Причина:** При отправке уведомлений нескольким получателям (владелец + менеджеры) ошибка у одного получателя останавливала обработку остальных.

**Проявление:**
- Если при отправке владельцу возникала ошибка → менеджеры не получали уведомления
- Если при отправке одному менеджеру возникала ошибка → остальные менеджеры не получали уведомления
- В методе `_send_bulk` отсутствовала обработка исключений

**Затронутые методы:**
- `notify_schedule_planned()` - уведомления о планировании смены
- `notify_shift_started()` - уведомления о начале смены
- `notify_shift_completed()` - уведомления о завершении смены
- `_send_bulk()` - массовая отправка уведомлений

### Проблема #2: Неправильная работа с event loop в Celery
**Файл:** `core/celery/tasks/notification_tasks.py`

**Причина:** Использование `asyncio.run()` в Celery задачах вызывало ошибки `RuntimeError('Event loop is closed')`, особенно при параллельной обработке нескольких уведомлений.

**Проявление:**
- Ошибка `RuntimeError('Event loop is closed')` в логах Celery worker
- Уведомления помечались как `FAILED` даже при успешной отправке
- Проблема усугублялась при массовой отправке уведомлений

**Затронутые задачи:**
- `send_notification_now()` - отправка одного уведомления
- `dispatch_scheduled_notifications()` - обработка запланированных уведомлений

---

## ✅ Исправления

### Исправление #1: Обработка исключений
**Коммит:** `642b489`  
**Дата:** 18.11.2025 03:18

**Изменения в `shared/services/shift_notification_service.py`:**

1. **Метод `notify_schedule_planned()`:**
   - Добавлена обработка исключений при отправке владельцу
   - Добавлена обработка исключений при получении списка менеджеров
   - Добавлена обработка исключений для каждого менеджера отдельно

2. **Метод `notify_shift_started()`:**
   - Аналогичные изменения

3. **Метод `notify_shift_completed()`:**
   - Аналогичные изменения

4. **Метод `_send_bulk()`:**
   - Добавлена обработка исключений при получении каналов для каждого пользователя
   - Добавлена обработка исключений при отправке каждому получателю
   - Ошибка у одного получателя не останавливает обработку остальных

**Пример исправления:**
```python
# БЫЛО:
await self._send(owner_id, NotificationType.SHIFT_STARTED, channels, title, message, data)
manager_user_ids = await self._get_managers_for_object(session, shift.object_id)
for manager_user_id in manager_user_ids:
    manager_channels = await self._get_channels(manager_user_id, NotificationType.SHIFT_STARTED)
    if manager_channels:
        await self._send(manager_user_id, NotificationType.SHIFT_STARTED, manager_channels, title, message, data)

# СТАЛО:
try:
    await self._send(owner_id, NotificationType.SHIFT_STARTED, channels, title, message, data)
except Exception as exc:
    logger.error(
        "Failed to send shift started notification to owner",
        owner_id=owner_id,
        shift_id=shift_id,
        error=str(exc),
    )

try:
    manager_user_ids = await self._get_managers_for_object(session, shift.object_id)
    for manager_user_id in manager_user_ids:
        try:
            manager_channels = await self._get_channels(manager_user_id, NotificationType.SHIFT_STARTED)
            if manager_channels:
                await self._send(manager_user_id, NotificationType.SHIFT_STARTED, manager_channels, title, message, data)
        except Exception as manager_exc:
            logger.error(
                "Failed to send shift started notification to manager",
                manager_user_id=manager_user_id,
                shift_id=shift_id,
                error=str(manager_exc),
            )
            continue
except Exception as exc:
    logger.error(
        "Failed to get managers or send notifications",
        shift_id=shift_id,
        error=str(exc),
    )
```

### Исправление #2: Правильная работа с event loop
**Коммит:** `7a8b970`  
**Дата:** 18.11.2025 04:00

**Изменения в `core/celery/tasks/notification_tasks.py`:**

1. **Метод `send_notification_now()`:**
   - Заменен `asyncio.run()` на правильную работу с event loop
   - Добавлена проверка состояния loop (закрыт/открыт)
   - Создание нового loop при необходимости

2. **Метод `dispatch_scheduled_notifications()`:**
   - Аналогичные изменения

**Пример исправления:**
```python
# БЫЛО:
import asyncio
return asyncio.run(dispatcher.dispatch_notification(notification_id))

# СТАЛО:
import asyncio

# Правильная работа с event loop в Celery
try:
    loop = asyncio.get_event_loop()
    if loop.is_closed():
        loop = asyncio.new_event_loop()
        asyncio.set_event_loop(loop)
except RuntimeError:
    loop = asyncio.new_event_loop()
    asyncio.set_event_loop(loop)

return loop.run_until_complete(dispatcher.dispatch_notification(notification_id))
```

**Обоснование:**
- В Celery worker может быть уже существующий event loop
- `asyncio.run()` создает новый loop и закрывает его после выполнения
- Это вызывает конфликты при параллельной обработке задач
- Правильный подход: проверка существующего loop, создание нового только при необходимости

---

## 📊 Результаты

### До исправлений:
- ❌ Ошибка у одного получателя останавливала отправку остальным
- ❌ Ошибки `RuntimeError('Event loop is closed')` в логах
- ❌ Уведомления помечались как `FAILED` без реальной причины
- ❌ Отправка уведомлений останавливалась после первой ошибки

### После исправлений:
- ✅ Ошибка у одного получателя не останавливает отправку остальным
- ✅ Правильная работа с event loop в Celery
- ✅ Все ошибки логируются с контекстом
- ✅ Уведомления отправляются стабильно, даже при ошибках отдельных получателей

---

## 🧪 Тестирование

### Проверка после исправлений:
1. **Создание уведомлений:**
   - Уведомления создаются в БД со статусом `PENDING`
   - Задачи `send_notification_now` ставятся в очередь Celery

2. **Отправка уведомлений:**
   - Уведомления успешно отправляются через Telegram
   - Статус обновляется на `SENT` после успешной отправки
   - Ошибки логируются, но не останавливают обработку

3. **Обработка ошибок:**
   - Ошибка у одного получателя не влияет на остальных
   - Все ошибки логируются с контекстом (user_id, notification_id, error)
   - Event loop корректно обрабатывается в Celery

---

## 📝 Рекомендации

1. **Мониторинг:**
   - Следить за логами на наличие ошибок отправки уведомлений
   - Проверять количество уведомлений со статусом `FAILED`
   - Мониторить производительность Celery worker

2. **Дальнейшие улучшения:**
   - Добавить retry механизм для failed уведомлений
   - Добавить метрики отправки уведомлений
   - Рассмотреть использование отдельной очереди для уведомлений

3. **Паттерны:**
   - Всегда обрабатывать исключения при массовой отправке
   - Использовать правильный подход к event loop в Celery задачах
   - Логировать все ошибки с контекстом для отладки

---

## 🔗 Связанные файлы

- `shared/services/shift_notification_service.py` - сервис уведомлений о сменах
- `core/celery/tasks/notification_tasks.py` - Celery задачи для уведомлений
- `shared/services/notification_dispatcher.py` - диспетчер уведомлений
- `shared/services/senders/telegram_sender.py` - отправщик Telegram уведомлений

---

## 📅 История изменений

- **18.11.2025 03:18** - Исправление #1: обработка исключений (коммит `642b489`)
- **18.11.2025 04:00** - Исправление #2: работа с event loop (коммит `7a8b970`)

