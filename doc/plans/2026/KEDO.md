ТЗ: Система заключения ЭТД с KYC и электронной подписью

Цель: Разработать архитектуру и workflow системы для заключения электронных трудовых договоров (ЭТД), включающей:

    Удаленную идентификацию (KYC) сотрудника для верификации профиля.

    Юридически значимое подписание документов усиленными подписями (УКЭП/УНЭП).

    Формирование архива согласно Приказу Минтруда № 578н.

1. Анализ законодательных ограничений (Контекст для нейросети)

    Трудовой кодекс РФ (ст. 22.3): Определяет перечень документов, подписываемых только усиленной электронной подписью (квалифицированной - УКЭП или неквалифицированной - УНЭП). Трудовой договор входит в этот перечень. Простая электронная подпись (ПЭП) для него неприменима.

    63-ФЗ «Об ЭП»: Регламентирует виды подписей. Для ПЭП необходимо выполнение условия: подпись позволяет идентифицировать лицо (например, через код из SMS + логин/пароль).

    152-ФЗ «О ПДн»: Обработка персональных данных возможна только с согласия субъекта. KYC-данные должны обрабатываться по принципу минимальной достаточности и удаляться после достижения цели.

    Приказ Минтруда № 578н: Устанавливает обязательный состав и формат электронного кадрового документа (номер, дата, код типа документа) и правила формирования архива.

    Указание Банка России № 3752-У: Определяет стандарты удаленной идентификации (KYC), которыми должны руководствоваться интегрируемые провайдеры (Госуслуги, СберID и др.).
    
2. Архитектура системы и модули
1. KYC-Верификация	Удаленная идентификация будущего сотрудника для заполнения профиля в системе и договора.	
- Интеграция с API ЕСИА (Госуслуги), СберID, VK ID.
- Запрос только необходимых атрибутов (ФИО, паспорт, СНИЛС, адрес).
- Не хранить полученные данные на сервере после сессии.
2. Подписание (Сторона Работодателя)	Формирование и подписание договора УКЭП.	
- Интеграция с криптопровайдером (КриптоПро, ViPNet CSP).
- Поддержка УКЭП, хранящейся на токене, в облаке (Госключ для ЮЛ) или в HSM.
3. Подписание (Сторона Работника)	Подписание договора сотрудником.	
- Основной поток: Интеграция с API «Госключ» для подписания УНЭП/УКЭП.
- Альтернатива для иных документов: Реализация ПЭП внутри системы (логин/пароль + код из SMS/приложения).
4. Архив и документооборот	Хранение, упаковка и предоставление доступа к подписанным документам.	
- Формирование пакета документа по 578н: PDF, SIG-файлы подписей, XML-протокол.
- REST API для выгрузки архива в корпоративное хранилище.
- Личные кабинеты для работника и работодателя.

3. Детальный Workflow процесса

Этап 0: KYC-Верификация профиля работника (НЕ для подписания)

    HR приглашает кандидата в систему, отправив уникальную ссылку.

    Кандидат переходит по ссылке, система предлагает пройти идентификацию через Госуслуги/СберID.

    После успешного KYC система временно заполняет шаблон профиля работника и отображает данные для проверки.

    Работник подтверждает корректность данных. Система создает верифицированный профиль, безвозвратно удаляя полученные из KYC «сырые» данные.

Этап 1: Инициация договора (Работодатель)

    HR выбирает шаблон договора и верифицированного сотрудника из списка.

    Система автоматически подставляет в шаблон данные из профиля сотрудника (ФИО, паспорт и т.д.).

    HR заполняет недостающие поля (должность, оклад) и нажимает «Подписать и отправить».

    Система формирует черновик договора, уполномоченное лицо (директор) подписывает его УКЭП через интеграцию с криптопровайдером.

    Система создает уникальную ссылку на подписание для сотрудника и отправляет уведомление.

Этап 2: Подписание договора (Работник)

    Работник переходит по ссылке, система перенаправляет его в мобильное приложение «Госключ» (где у него уже выпущена УНЭП/УКЭП).

    Работник подтверждает подписание в приложении за секунды (интеграция по схеме API-ключа, как в статье).

    Факт подписания фиксируется системой, подписанный документ возвращается.

Этап 3: Заверение и архив

    Система формирует юридически значимый пакет документа:

        PDF-документ с визуальным отображением подписей.

        SIG-файл с подписью работодателя.

        SIG-файл с подписью работника (из Госключа).

        XML-протокол с реквизитами по Приказу 578н.

    Пакет рассылается сторонам и помещается в электронный архив.

    Работодатель получает уведомление о подписании, работник — о доступе к своему экземпляру в ЛК.

4. Ключевые интеграции и технические требования

    Интеграция с KYC-провайдерами: Использовать OAuth 2.0/OpenID Connect протоколы для запроса атрибутов. Пример запроса к ЕСИА: scope=openid fullname birthdate passport_data snils.

    Интеграция с «Госключ»: Реализовать по схеме, описанной в статье: получить обезличенную подпись и API-ключ для организации, использовать REST API для создания операций подписания.

    Интеграция с криптопровайдером: Использовать библиотеки (CAdES, XAdES) для работы с УКЭП. Предусмотреть обработку ошибок (просрочка лицензии, токена).

    Формат архива: Строгое соответствие XML-схеме из Приказа 578н. Автоматическая генерация кодов типов документов.

    Безопасность: Все передачи данных — по HTTPS. KYC-данные не пишутся в логи БД. Применение принципа zero-knowledge для персональных данных.



Хорошо, пользователь хочет понять, как изменится архитектура его системы управления персоналом для заключения электронных трудовых договоров в связи с возможным внедрением мессенджера Max с сентября 2026 года. Вопрос очень конкретный и касается юридических и технических аспектов.

Важная деталь из : сейчас документы на подпись от коммерческих организаций (включая работодателей) должны направляться через Max, а подписание происходит в «Госключе». Это означает, что интеграция с API Max становится обязательной для отправки документов, но сам процесс подписания остается прежним.

хорошо объясняет техническую сторону: Max будет использовать УНЭП/УКЭП из «Госключа», а не собственную подпись. Это ключевой момент для архитектуры — система должна поддерживать два сценария: стандартный (через «Госключ») и новый (через Max → «Госключ»).

Сравнивая с более ранним ТЗ, вижу, что основные изменения касаются именно этапа отправки документов работнику. Теперь нужно добавить маршрутизацию через Max API, сохранив всю остальную логику (KYC, шаблоны договоров, подписание работодателем, архив).

Стоит отметить, что KYC-модуль не затрагивается этими изменениями, так как он используется для первичной верификации профиля, а не для подписания документов. Это важно подчеркнуть.

Пользователь, скорее всего, разработчик или системный архитектор, который хочет заранее спроектировать систему с учетом грядущих изменений. Ему нужно четкое техническое решение, а не общие рассуждения о законопроектах.

В указано, что возможность подписания в самом Max еще не реализована, поэтому в текущей архитектуре нужно ориентироваться на схему "отправка через Max → подписание в Госключе".

Лучше всего будет визуализировать изменения в workflow через сравнительную таблицу, так как пользователь уже знаком с предыдущим детальным описанием процесса. Это позволит быстро выделить ключевые отличия. В таблице стоит разделить этапы, показав, что KYC и архивирование не меняются, а изменения касаются отправки уведомлений и процесса подписания для работника. После таблицы нужно дать конкретные рекомендации по интеграции API Max и адаптации системы, основываясь на информации из о необходимости перехода с API «Госключа» на API Max для отправки документов.

Стоит также упомянуть, что это добровольная опция , поэтому система должна поддерживать оба сценария — с Max и без него. Это важно для гибкости внедрения.

Введение мессенджера Max с 1 сентября 2026 года изменит не столько ядро вашей системы (KYC, шаблоны, архивы), сколько канал коммуникации и некоторые этапы workflow. Инициатива не отменяет действующие способы и является добровольной для сторон.
🔄 Сравнение workflow: текущий и новый сценарий с Max

Схема ниже показывает изменения на примере процесса заключения договора.
Этап		
0. KYC-верификация	
Текущий сценарий (до 01.09.2026): Система предлагает пройти идентификацию через Госуслуги/СберID для создания профиля. Данные удаляются после сессии.	
Новый сценарий (после 01.09.2026, при использовании Max): Без изменений. KYC остается независимым процессом для верификации профиля.
1. Подготовка договора
Текущий сценарий (до 01.09.2026): HR формирует договор, подписывает его УКЭП (через КриптоПро/токен).	
Новый сценарий (после 01.09.2026, при использовании Max): Без изменений. Подготовка и подписание работодателем внутри системы не меняются.
2. Отправка работнику	
Текущий сценарий (до 01.09.2026): Система отправляет работнику ссылку для подписания (email, SMS, внутренний ЛК).	
Новый сценарий (после 01.09.2026, при использовании Max): Система через API Max отправляет работнику уведомление о документе в мессенджер. Ссылка ведет в «Госключ».
3. Подписание работником	
Текущий сценарий (до 01.09.2026): Работник переходит по ссылке и подписывает договор УНЭП в приложении «Госключ».
Новый сценарий (после 01.09.2026, при использовании Max): Работник получает уведомление в Max, откуда перенаправляется в «Госключ» для подписания.
4. Архив и хранение	
Текущий сценарий (до 01.09.2026): Система формирует пакет (PDF, SIG, XML) согласно Приказу 578н.	
Новый сценарий (после 01.09.2026, при использовании Max): Без изменений. Возврат подписанного документа и архивация происходят по отработанной схеме.

🏗️ Рекомендации по обновлению архитектуры

Для поддержки нового сценария потребуются следующие изменения:

    Добавление модуля интеграции с API Max. Его основная задача — отправка уведомлений о готовых к подписанию документах. Нужно реализовать безопасный обмен данными между вашей системой и мессенджером. Интеграция с «Госключом» для самого акта подписания сохранится.

    Адаптация процесса отправки документов. Алгоритм должен определять, какой канал (Max, email, иной) использовать для конкретного работника, исходя из его предпочтений и политики компании, так как использование Max — добровольное.

    Обновление пользовательского интерфейса. В интерфейс для HR и работников нужно добавить настройки выбора канала связи и статусы доставки документов через Max.

