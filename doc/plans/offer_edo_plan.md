# План: Оферта + KYC ЕСИА + ПЭП подписание

**Дата:** 2026-02-15  
**Статус:** Черновик  
**Контекст:** Расширение Модуля 4 (Кадровое ЭДО) из `restruct.md`

## Предпосылки и блокеры

| # | Задача | Статус | Блокер |
|---|--------|--------|--------|
| 1 | Регистрация ИС в ЕСИА (тестовый контур) | ❌ Не начато | Заявка в Минцифры, 2-4 недели |
| 2 | Серверная лицензия КриптоПро CSP | ❌ Нужна | Триал 3 мес. для разработки |
| 3 | Сертификат ЭП для ЕСИА | ✅ Есть | — |
| 4 | S3-хранилище | ✅ Работает | MinIO (dev), reg.ru S3 (prod) |

**Важно**: Эпики 1, 3, 5 можно делать параллельно с ожиданием регистрации ЕСИА. Эпик 2 (ЕСИА) — только после получения client_id.

---

## Эпик 1: Тип договора «Оферта» + конструктор

### 1.1. Справочник типов — seed-миграция

- Добавить `ContractType(code="offer", label="Оферта (договор присоединения)")` 
- Миграция Alembic с INSERT

### 1.2. ConstructorFlow для оферты

Структура шагов конструктора на основе `doc/text/offer.md`:

| # | slug | title | Плейсхолдеры | Примечание |
|---|------|-------|-------------|-----------|
| 1 | `company_info` | Реквизиты заказчика | `{{ company_name }}`, `{{ ogrn }}`, `{{ inn }}`, `{{ legal_address }}`, `{{ ceo_name }}`, `{{ ceo_basis }}` | Из LegalProfile/SoleProprietorProfile владельца |
| 2 | `site_info` | Сайт и приложения | `{{ site_url }}`, `{{ app_names }}` | URL платформы владельца |
| 3 | `service_types` | Перечень услуг (п. 5.6) | `{{ service_list }}` | Динамический список: чекбоксы + свободное поле. Меняется от оферты к оферте |
| 4 | `quality_requirements` | Требования к качеству | `{{ quality_url }}` | Ссылка на внутренние правила |
| 5 | `payment_terms` | Стоимость и оплата | `{{ ndfl_rate }}`, `{{ payment_period }}` | По умолчанию 13% НДФЛ |
| 6 | `penalties` | Штрафы и неустойки | `{{ penalty_details_url }}` | Ссылка на таблицу штрафов |
| 7 | `confidentiality` | Конфиденциальность | `{{ confidentiality_penalty }}` | Сумма штрафа (п. 12.4) |
| 8 | `jurisdiction` | Подсудность | `{{ court_name }}` | Название суда (п. 11.3) |
| 9 | `personal_data` | Персональные данные | — | Фиксированный текст (п. 12.4-12.5) |

- Миграция: создание `ConstructorFlow`, `ConstructorStep`, `ConstructorFragment` для каждого шага
- Фрагменты содержат текст разделов оферты с `{{ плейсхолдерами }}`
- Шаг `service_types` — schema с полем `fields: [{key: "services", type: "checkbox_group", options: [...]}]`

### 1.3. Особенности типа «Оферта» в ContractTemplate

- Определение через `contract_type.code == "offer"` (без нового поля)
- Один шаблон-оферта → множество Contract (договор присоединения)
- Текст оферты фиксируется в `ContractTemplate.content` при создании через конструктор
- При создании `Contract` по оферте — контент копируется, реквизиты сотрудника подставляются при акцепте

### 1.4. Множественные оферты

- Один владелец может иметь несколько оферт (разные типы услуг/объектов)
- В UI списка шаблонов: фильтр по типу "Оферта"
- Связь с объектами: опциональное поле `ContractTemplate.applicable_objects` (JSON, список object_id)

### 1.5. Автоакцепт изменений

- При изменении текста оферты (новая версия шаблона) — уже подписанные Contract **не меняются**
- Новые подписания — по новой версии
- Уведомление существующим исполнителям об изменении условий (п. 10.3-10.4 оферты)

**Файлы:**
- `domain/entities/contract_type.py` — без изменений (seed-данные)
- `domain/entities/constructor_flow.py` — без изменений структуры
- `apps/web/services/constructor_service.py` — доработка `build_template()` для подстановки данных из профиля владельца
- Миграция Alembic (seed)

---

## Эпик 2: KYC через ЕСИА + КриптоПро

### 2.1. Docker-сервис КриптоПро (подпись ГОСТ)

- Форк/использование [waves-enterprise/cryptopro-sign](https://github.com/waves-enterprise/cryptopro-sign)
- Добавить в `docker-compose.dev.yml`:
```yaml
cryptopro-sign:
  build:
    context: ./infrastructure/cryptopro
    args:
      CERTIFICATE_PIN: ${CRYPTOPRO_PIN}
      ESIA_ENVIRONMENT: test
  ports:
    - "3037:3037"
```
- На проде: отдельный контейнер, `ESIA_ENVIRONMENT=prod`
- Лицензия: триал 3 мес. для разработки → серверная для прода

### 2.2. ЕСИА-клиент (Python)

Файл: `infrastructure/external/esia/esia_client.py`

```python
class EsiaClient:
    """OAuth2 клиент для ЕСИА (Госуслуги)."""
    
    def __init__(self, esia_host, client_id, scope, cryptopro_url, public_key_path):
        ...
    
    async def sign_params(self, params: dict) -> dict:
        """Подпись параметров через cryptopro-sign сервис."""
        # POST cryptopro_url/cryptopro/sign
        # text = scope + timestamp + client_id + state
        ...
    
    async def get_auth_link(self, redirect_url: str) -> str:
        """Ссылка на авторизацию ЕСИА."""
        # GET esia_host/aas/oauth2/ac?...signed_params
        ...
    
    async def get_tokens(self, code: str) -> dict:
        """Обмен code на id_token + access_token."""
        # POST esia_host/aas/oauth2/te?...signed_params
        ...
    
    async def get_user_info(self, id_token, access_token) -> dict:
        """Получение ФИО, email, trusted-статус."""
        # GET esia_host/rs/prns/{oid}
        # GET esia_host/rs/prns/{oid}/ctts?embed=(elements)
        ...
```

Конфигурация (env):
```
ESIA_HOST=https://esia-portal1.test.gosuslugi.ru
ESIA_CLIENT_ID=STAFFPROBOT
ESIA_SCOPE=openid email fullname
ESIA_PUBLIC_KEY_PATH=/app/infrastructure/cryptopro/esia/esia_prod.pub
CRYPTOPRO_SERVICE_URL=http://cryptopro-sign:3037
CRYPTOPRO_PIN=12345678
```

### 2.3. Реализация GosuslugiKycProvider

Файл: `shared/services/kyc_service.py` — замена заглушки

```python
class GosuslugiKycProvider(KycProvider):
    provider_key = "gosuslugi"
    
    async def start_verification(self, profile):
        esia = EsiaClient(...)  # из settings
        redirect_url = f"{web_url}/auth/esia/callback?profile_id={profile.id}"
        auth_link = await esia.get_auth_link(redirect_url)
        return {"provider": "gosuslugi", "redirect_url": auth_link}
    
    async def complete_verification(self, profile, payload):
        esia = EsiaClient(...)
        tokens = await esia.get_tokens(payload["code"])
        user_info = await esia.get_user_info(tokens["idToken"], tokens["accessToken"])
        # Обновить IndividualProfile из данных ЕСИА
        # Вернуть True если trusted
        return user_info.get("trusted", False)
```

### 2.4. Callback-роут ЕСИА

Файл: `apps/web/routes/auth.py` — добавить endpoint

```
GET /auth/esia/callback?code=...&state=...&profile_id=...
```

Логика:
1. Валидация state (CSRF)
2. `esia.get_tokens(code)`
3. `esia.get_user_info(tokens)`
4. `kyc_service.mark_verified(profile_id, user_info)`
5. Редирект на профиль с сообщением об успехе/ошибке

### 2.5. Блокер: регистрация в ЕСИА

Для получения client_id:
1. Подать заявку на подключение ИС в тестовой среде ЕСИА
2. Получить мнемонику ИС (client_id)
3. Загрузить сертификат ЭП
4. Настроить redirect_uri

**До получения client_id** — разработка ведётся с mock-провайдером (текущая заглушка).

---

## Эпик 3: Воркфлоу присоединения к оферте

### 3.1. Новый статус Contract

Добавить в `Contract.status`:
- `pending_acceptance` — ожидание акцепта сотрудника

Переходы:
```
draft → pending_acceptance → active
                           → rejected (сотрудник отказался)
```

### 3.2. Создание договора (владелец/управляющий)

UI: `/owner/contracts/create`

1. Выбор шаблона → если тип "Оферта" → особый flow
2. Выбор сотрудника
3. `Contract.status = "pending_acceptance"`
4. Отправка уведомления сотруднику (ТГ + in-app): "Вам направлена оферта для подписания"

### 3.3. Заполнение реквизитов сотрудником (самостоятельно)

Сотрудник заполняет свой профиль (`IndividualProfile`) **до или в момент** подписания.

Обязательные поля для подписания оферты (из п. 12.5 оферты):
- ФИО (`last_name`, `first_name`, `middle_name`)
- Пол, дата рождения (`gender`, `birth_date`)
- Паспорт (`passport_series`, `passport_number`, `passport_issued_at`, `passport_issued_by`)
- Адрес регистрации (`registration_address_id`)
- Телефон (`phone`)
- Email (`email`)
- СНИЛС (`snils`)
- Расчётный счёт (`account_number`) — уже есть в `BankDetailsMixin`
- БИК (`bik`) — уже есть в `BankDetailsMixin`

UI: существующая страница профиля `/employee/profile` — добавить валидацию заполненности перед подписанием.

### 3.4. Акцепт сотрудником (веб-профиль)

Роут: `GET /employee/contracts/{id}/accept`

Шаги:
1. Отображение текста оферты (read-only)
2. Проверка заполненности обязательных реквизитов в `IndividualProfile`:
   - **Не заполнены** → "Для подписания необходимо заполнить реквизиты в профиле" + список незаполненных полей + кнопка "Перейти в профиль"
   - **Заполнены** → кнопка "Принять оферту"
3. Нажатие "Принять оферту" → инициация ПЭП

> **Примечание**: KYC через ЕСИА — опциональная верификация, не блокирует подписание. Может быть включена позже как дополнительное требование для отдельных типов оферт.

### 3.4a. Бейдж верификации KYC в профилях

На профилях **сотрудника** и **владельца** отображать статус KYC:
- `profile.kyc_status == "verified"` → зелёный бейдж "✓ Верифицирован через Госуслуги" (с датой `kyc_verified_at`)
- `profile.kyc_status == "pending"` → жёлтый бейдж "Верификация в процессе"
- `profile.kyc_status == "unverified"` → серый бейдж "Не верифицирован" + ссылка "Пройти верификацию"
- `profile.kyc_status == "failed"` → красный бейдж "Верификация не пройдена" + ссылка "Повторить"

Где отображать:
- `/employee/profile` — профиль сотрудника (свой)
- `/owner/profile/profiles` — профили владельца (свои)
- Карточка сотрудника в списке владельца — иконка верификации рядом с именем
- Карточка договора — бейдж у имени подписанта

### 3.5. ПЭП — Простая электронная подпись (абстракция каналов)

Файл: `shared/services/pep_service.py`

```python
class PepChannel(ABC):
    """Абстрактный канал доставки OTP для ПЭП."""
    channel_key: str
    
    async def send_otp(self, user, code: str) -> bool: ...
    
class TelegramPepChannel(PepChannel):
    """OTP через Telegram-бот."""
    channel_key = "telegram"
    
    async def send_otp(self, user, code: str) -> bool:
        # Отправка через бот: "Код подтверждения подписания договора: {code}"
        ...

class SmsPepChannel(PepChannel):
    """OTP через SMS (для продакшн)."""
    channel_key = "sms"
    # Реализация позже

class PepService:
    """Сервис простой электронной подписи."""
    
    def __init__(self, channel: PepChannel = None):
        self.channel = channel or TelegramPepChannel()
    
    async def initiate_signing(self, user_id, contract_id) -> str:
        """Генерация OTP, сохранение в Redis, отправка через канал."""
        code = generate_otp(6)
        # Redis: pep:{user_id}:{contract_id} = {code, expires, attempts}
        await self.channel.send_otp(user, code)
        return "sent"
    
    async def verify_otp(self, user_id, contract_id, code) -> bool:
        """Проверка OTP. Макс 3 попытки, TTL 5 мин."""
        ...
```

### 3.6. Фиксация подписания

При успешной верификации OTP:

1. **Подстановка реквизитов сотрудника** в текст договора:
   - Из `IndividualProfile`: ФИО, пол, возраст, дата/место рождения, паспорт (серия, номер, кем выдан, дата), адреса (регистрация, проживание), телефон, email, СНИЛС, банковские реквизиты
   - Формат подстановки: шаблон Jinja2 в `Contract.content`

2. **Генерация PDF** подписанного договора:
   - Файл: `shared/services/contract_pdf_service.py`
   - Использовать `weasyprint` (HTML → PDF)
   - В PDF включить: текст договора + блок «Подписано ПЭП» (дата, метод, хеш OTP, идентификатор ЕСИА)
   - Зависимость: `weasyprint` в `requirements.txt`

3. **Сохранение PDF в S3**:
   - Использовать существующий `S3MediaStorageClient`
   - Папка: `contracts/{contract_id}/signed_{version}.pdf`
   - Сохранить ключ S3 в `ContractVersion` (новое поле `file_key`)
   
4. **Обновление Contract**:
   - `status = "active"`
   - `signed_at = now()`
   - `values` — дополнить реквизитами сотрудника
   - Создать `ContractVersion` с финальным текстом + `file_key`
   - Запись в `ContractHistory(change_type="signed")`

5. **Уведомление обеим сторонам**:
   - Владельцу: "Сотрудник {name} подписал оферту «{title}»"
   - Сотруднику: "Вы успешно подписали оферту «{title}». Документ доступен в личном кабинете"

### 3.7. Изменения в моделях

```python
# ContractVersion — новое поле
file_key = Column(String(500), nullable=True, comment="Ключ подписанного PDF в S3")

# Contract — новое поле для метаданных ПЭП
pep_metadata = Column(JSON, nullable=True, comment="Метаданные ПЭП: channel, otp_hash, esia_oid, signed_ip")
```

Миграция Alembic.

---

## Эпик 4: Уведомления

### 4.1. Новые типы

Добавить в `NotificationType`:
```python
# Оферта
OFFER_SENT = "offer_sent"                     # Оферта направлена на подписание
OFFER_ACCEPTED = "offer_accepted"             # Оферта принята сотрудником
OFFER_REJECTED = "offer_rejected"             # Сотрудник отклонил оферту
OFFER_TERMS_CHANGED = "offer_terms_changed"   # Условия оферты изменены

# KYC / Верификация
KYC_REQUIRED = "kyc_required"                 # Требуется верификация
KYC_VERIFIED = "kyc_verified"                 # Верификация пройдена
KYC_FAILED = "kyc_failed"                     # Верификация не пройдена
```

### 4.2. Шаблоны

Добавить в `base_templates.py`:
- `OFFER_SENT` → ТГ: "📋 Вам направлена оферта «$offer_title» от $company_name. Подпишите в личном кабинете: $accept_url"
- `OFFER_ACCEPTED` → ТГ: "✅ Сотрудник $employee_name принял оферту «$offer_title». Документ: $document_url"
- `OFFER_TERMS_CHANGED` → ТГ: "📋 Условия оферты «$offer_title» обновлены. Изменения вступают в силу автоматически (п. 10.3)"
- `KYC_REQUIRED` → ТГ: "🔐 Для подписания договора необходимо пройти верификацию через Госуслуги: $kyc_url"

### 4.3. Регистрация в NotificationTypeMeta

- Seed-миграция с новыми типами
- `category = "contracts"` для offer-*, `category = "verification"` для kyc-*
- `is_user_configurable = True`
- `available_channels = ["telegram", "inapp"]`

### 4.4. Интеграция с настройками `/owner/notifications`

- Новые типы отображаются в UI настроек владельца
- Проверить, что `CONTRACT_SIGNED` корректно срабатывает для оферт (через общий flow)
- `CONTRACT_EXPIRING` — для оферт не применяется (бессрочные), проверить фильтрацию

---

## Порядок реализации

```
Фаза A (можно начинать сейчас):
  1. Эпик 1: Тип «Оферта» + конструктор     — 1-2 недели
  2. Эпик 4: Уведомления                      — 3-5 дней
  
Фаза B (параллельно с заявкой на ЕСИА):
  3. Эпик 3 без KYC-проверки:                 — 1-2 недели
     - Статус pending_acceptance
     - ПЭП через Telegram (абстракция каналов)
     - Генерация PDF + S3
     - Фиксация подписания
  
Фаза C (после получения client_id ЕСИА):
  4. Эпик 2: ЕСИА + КриптоПро                — 1-2 недели
  5. Интеграция KYC-проверки в flow акцепта
  
Фаза D (перед продом):
  6. Серверная лицензия КриптоПро
  7. SMS-канал ПЭП
  8. Нагрузочное тестирование
```

**Общая оценка**: 5-7 недель (при параллельной работе с регистрацией ЕСИА)

---

## Риски

| Риск | Вероятность | Митигация |
|------|------------|-----------|
| Задержка регистрации ЕСИА | Высокая | Фазы A+B не зависят от ЕСИА |
| Проблемы с КриптоПро в Docker | Средняя | Тестовый сертификат через certsrv |
| Юридическая валидность ПЭП через ТГ | Средняя | SMS-канал как fallback для прода |
| weasyprint — проблемы со шрифтами | Низкая | Подключить шрифты через Docker |

---

## Зависимости (новые пакеты)

- `weasyprint` — генерация PDF
- `pyjwt` — декодирование id_token ЕСИА (если не используется через cryptopro)
- `boto3` — уже есть в requirements.txt
